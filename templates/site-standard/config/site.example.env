# Example only — never commit real secrets.
# Copy to host shared path outside git.

NODE_ENV=production
PORT=3100
HOSTNAME=127.0.0.1
# DATABASE_URL=
# ADMIN_SESSION_SECRET=
# NEXT_PUBLIC_SITE_URL=https://example.ir
