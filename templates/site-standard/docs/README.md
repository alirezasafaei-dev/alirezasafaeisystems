# Site documentation index

Minimum docs for every site:

- Architecture one-pager
- Deploy notes (`../deploy/README.md`)
- Rollback notes (`../rollback/README.md`)
- Monitoring notes (`../monitoring/README.md`)
- Incident contacts (aliases only)

Reports of live deploys go to platform `docs/reports/`, not only site-local trees.
